package seedu.canvas;

import javafx.application.Application;

public class CanVasLauncher {

    /**
     * Launches the Canvas Application.
     *
     * @param args
     *  Standard arguments for running
     */
    public static void main(String[] args) {
        Application.launch(CanVas.class, args);
    }
}
